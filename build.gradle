
/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn more about Gradle by exploring our samples at https://docs.gradle.org/7.6/samples
 */
plugins {
    id 'application'
    id 'eclipse'
    id 'java'
}

application {
    mainClass = 'com.github.huymaster.Main'
}

sourceCompatibility = 19
targetCompatibility = 19

project.version = '1.0.0'


repositories {
    mavenCentral()
}

dependencies {
}

if (hasProperty('buildScan')) {
    buildScan {
        termsOfServiceUrl = 'https://gradle.com/terms-of-service'
        termsOfServiceAgree = 'yes'
    }
}

defaultTasks 'jar'

run {
	dependsOn 'jar'
}

jar {
	manifest { 
    	attributes "Main-Class": "com.github.huymaster.Main"
  	}
  	
	from {
		duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    	configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
  	}
	println('Exported version: ' + project.version)
}